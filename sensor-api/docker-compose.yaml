services:
  webserver:
    build:
      context: .
      dockerfile: lgpio.Dockerfile
    privileged: true
    command: ['python', '-u', 'main.py']
    volumes:
      - .:/usr/src/app/
    ports:
      - 8080:8080
    labels:
      logging_jobname: sensor-api
    restart: always
